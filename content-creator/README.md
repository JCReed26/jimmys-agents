# Conent-Creator 

**this would be way easier in langgraph with langchain new idea**
**langsmith could be great in optimizing copy/paste structure for each account**

Simple workflow agent 

Sequential Workflow ADK Agent
ContentIdeaGeneration(x3) --> Human-In-Loop-Telegram-Verify --> SplitIdeasForParallelExecution --> IdeaToVideoPrompt(static(images, stock photos) and editor(json2video)) --> StaticContentGeneror --> json2videoEDITORCreatorAgent --> parallelExecutionCombined --> Human-In-Loop-Telegram-Verify --> Upload2GetLate --> postScheduler --> PostFromGetLate2AllPlatforms.

CRON
--> This will be scheduled to run daily at 7am
--> Will generate 3 pieces of content parallelized in generation
--> Will post on schedule at certain times 

### APIs and Integrations 
getlate.dev
json2video